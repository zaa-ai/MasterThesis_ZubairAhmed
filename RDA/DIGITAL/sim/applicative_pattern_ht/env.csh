setenv TESTCASE applicative_pattern
setenv UVM_TESTCASE top_test
setenv TC_DESCRIPTION "Simulate the applicative pattern for pattern generation."
setenv VCS_OPTIONS "$VCS_OPTIONS +uvm_set_type_override=top_default_seq,applicative_pattern_ht_seq"
# do not forget the newline at the end
